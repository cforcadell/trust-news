
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../configmap.yaml
  - ../../service.yaml
  - ../../deployment.yaml

# Reemplaza el comando manual: kubectl create secret generic mongodb-app-secret ...
# secretGenerator:
# - name: mongodb-app-secret
#   namespace: apis
#   envs:
#     - prod-mongodb-app.env
patches:
  - path: patch-resources.yaml 
generatorOptions:
  disableNameSuffixHash: true