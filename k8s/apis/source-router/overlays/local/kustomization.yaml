apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

secretGenerator:
  - name: source-router-enc
    namespace: apis
    envs:
      - source-router.env

generatorOptions:
  disableNameSuffixHash: true

namespace: apis
