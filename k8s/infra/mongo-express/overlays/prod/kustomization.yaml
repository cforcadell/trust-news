apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

# En prod crea el secreto fuera del repo, por ejemplo:
# kubectl create secret generic mongo-express-secret \
#   --namespace infra \
#   --from-env-file=mongo-express.env

namespace: infra
