
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:


# Reemplaza el comando manual: kubectl create secret generic mongodb-secret ...
secretGenerator:
- name: ethereum-secrets
  namespace: blockchain
  envs:
    - ethereum.env

generatorOptions:
  disableNameSuffixHash: true