# construye nueva imagen con tag generate-asertions-api ( en lugar de latest)
docker build -t generate-asertions-api .
# listado de imagenes
docker images
# borrar una imagen
docker rmi id_image
# Ejecuta conetnedor a partir de la imagen pasada como parametro y con el nombre name 
docker stop generate-asertions-api
docker remove generate-asertions-api
docker run -d --name generate-asertions-api -p 8090:8090 generate-asertions-api
# Listado de contenedores en ejecución
docker ps

# Listado de contenedores activos
docker ps -a 
# borrar contenedor 
docker remove nombre_contenedor || container_id



curl -X POST "http://127.0.0.1:8090/extraer" \
     -H "Content-Type: application/json" \
     -d '{"texto": "Catalunya tiene una población de 8 millones de habitantes y 2 millones de poblacion extranjera"}'