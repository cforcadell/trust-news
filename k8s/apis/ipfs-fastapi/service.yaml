apiVersion: v1
kind: Service
metadata:
  name: ipfs-fastapi
  namespace: apis
spec:
  selector:
    app: ipfs-fastapi
  ports:
    - protocol: TCP
      port: 8060
      targetPort: 8060
  type: ClusterIP