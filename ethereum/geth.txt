Para que el nodo 2 se conecte al 1:

Levanta el primero:

docker compose up -d geth-node1


Obtén el enode:

docker exec geth-node1 geth attach http://localhost:8545 --exec "admin.nodeInfo.enode"


Copia la cadena resultante (por ejemplo):

enode://abcdef1234...@geth-node1:30303


Guárdala en tu host en:

./ethereum/node1_enode.txt


Luego levanta todo:

docker compose up -d


Así, el segundo nodo leerá automáticamente el enode desde /ethereum/node1_enode.txt.

🧱 Estructura final
project-root/
├── docker-compose.base.yml
├── docker-compose.blockchain.yml
├── ethereum/
│   ├── genesis.json
│   ├── passwords.txt
│   └── node1_enode.txt   ← se genera después del primer arranque
└── volumes/
    ├── geth/
    │   ├── node1/
    │   └── node2/


¿Quieres que te prepare también un pequeño script (init_enode.sh) que ejecute los pasos 1–3 automáticamente y cree el node1_enode.txt por ti tras levantar el primer nodo?