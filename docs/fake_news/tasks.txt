
next version Develop v0.0.8 OK
- Dockerfile optimizado y securizado OK 
- Integracion con keycloak OK 
- nuevo gateway con apis publicas e integrado con keycloak OK
- nginx.conf solo usa gateway OK
- kafka sin zookeeper. Solo kraft OK 

- Validacion de Issuer. OK
- Strict hostname y https. OK
- mover toda la config volatil a .env. Crear .env examples al no poder subir el bueno (k8s) OK
- Validar llamadas a nuevo-post, getorder a nivel de api con obtencio de token oaut. OK Incorporar a tests automaticos OK
- Preparar despliegue en prod OK


Develop v0.0.9 Rate Limit RELEASED
- keys & rate limit control OK
- usuarios con limite de consumo con generate asertions y validations OK
- new admin module to control new users OK
- updated python tests OK 
- autofilter client_id in gateway for all orders endpoints OK
- check autofilter for FE. Restrict order visibility OK

Develop v0.0.9.1
- Really decrement quotas on usage. news handler on getting events and generating assertions

Develop v0.0.10. Validators Config and new FE style

- validators config in chain
    - Blockchain: new ipfs-config in contract.
        - update registrattion config to write ipfs-config-hash -> event new_validator_config.
        - new update-config function to write ipfs-config-hash  -> event new_validator_config, Change what is necessary in the registration process of the validatot
        - json { name: str,
                 type: enum (1: "General_AI,", 2: "Trained_IA",3:"Dedicated_Agent",4:"Human") 
                 , provider: str,
                 , model: str,
                 , active_date: date
                 , updated_date: date,
                 , end_date:date
                 , status: enum(1:"Registered",2:"Unregistered",3:"Banned")
                 }
        - new validators function: returns validators with its ipfs_hashes
    - validate-assertions:
        - call update-config smart contract function startup if ipfs-hash config differts from registered in blockchain even the valiator is already registered. Fon non registered validators add hash-ipfs config logig in register.
        - call update-config smart contract function on configuration change (@app.put("/admin/config", tags=["Admin"]))
        - call update-config smart contract function on desregistration endpoint call. Write end_date
    - news_chain:
        -  recovers validators functions as new endpoints 
        - listen new_validator_config anf convert to kafka event reading ipfs data.     
    - news_handler: Validators cache    
        - On startup asks news validators function (news-chain) and cache validators
        - listen event new_validator_config and update cache.
        - on getting new validation assertion record into mongodb validator config (it can change) related to the validation event. 
        - new list validators cache endpoint that gets blockchain validators and  revovers ipfs data
        - new get validator with validations cache endpoint (same behaviour of last endpoint). We want to filter by validator hash, provider, model. Optional parameters (default no) that allows to recover validations made with link to order_id
        
    - Gateway (add token security to new endpoints as existing endpoints):
        - route new list validators cache endpoint.
        - route new get validator cache endpoint .
        - route new get validator with validations 

    - FE: 
        - Let click on validator hash  to see his config
        - new validators item on main menu to list them on a table  with detailed view on click.
        - For each validator, button to see validations made (show order_id and link to the order) keeping the format of the validations tree in detailed order
    - Pydantic models:
        - Adapt models (every pydntic model must be declared in common module )

feat: add assertion protocol v2 and evidence-aware validation

Introduce enriched assertion documents with protocol v2 payloads shared across
generation, validation, LIGHT mode and blockchain flows. Add contextual fields
for evidence search, domain routing profiles, cache support and RAG evidence
responses while keeping blockchain registration based on categoryId.

Update news-handler, news-chain, validate-asertions and evidence-search to use
the new payload shape, persist validation evidence metadata, refresh validator
category routing, and document the updated async architecture and Skaffold/K8s
configuration.



Develop v0.0.11.

- endpoints de recomendación de modelos apirouter economicos OK 
- cambiar login  skin OK
- Restylish de FE
- cambiar resultado validacion con mejoras visuales OK
- soporte multiidioma (castellano + ingles) OK
- Nueva monitotizacion de mongodb +  Separacion de usuarios admin/user + uso de appuser en apis OK
- Nueva monitotizacion de kakfa (kafdrop) OK
- Refactorizaoin de modelos pydantic y funciones comunes OK
- Nueva categorizacion de validadores: OK
    LLM_MEMORY_VALIDATION = 1
    LLM_SEARCH_VALIDATION = 2
    RAG_EVIDENCE_VALIDATION = 3
    DETERMINISTIC_VALIDATION = 4 --> - Validadores AI, como hasta ahora, con entrenamiento adhoc
    HUMAN = 5

- Nuevos validadores RAG(RAG_EVIDENCE_VALIDATION):
        - nuevo servicio evidence search basado en buscador externo.  
            - Abstraer proveedor de busqueda e incorporar exa OK
            - Cargador de config de preferred domains OK 
            - Soporte a multiples perfiles de predefined domains OK
            - Cacheo de resultados OK
            - en lugar de guardar snippet hacer download y guardar chunks que se inyectan al validador OK

        - Nueva Configuracion de validadores 
            - levantar 1 validador RAG_EVIDENCE_VALIDATION escuchando a todas las categoria con preferred domains bien configurado. Modelos LLM simples  RAG_EVIDENCE_VALIDATION 
                - Configurar un profile con muchas más url configuradas
            - 2  validadores RAG_EVIDENCE_VALIDATION registrado a todas las categorias sin preferred domains. Modelos LLM simples diferentes que validen las mismas evidencias OK
            


    - Nuevos validadores con búsqueda online (LLM_SEARCH_VALIDATION) OK
    - en modo light no enviar validacion si los validadores no responden a health chechk Pte Test
-Estandarizacion de categorias en base a su categoryId a lo largo de toda la cadena
    

Hecho. Añadí el healthcheck y el filtro previo en modo light.

Cambios principales:

validate-asertions ahora expone GET /health en main.py (line 959).
La config pública del validador incluye service_url, publicada en IPFS/Kafka desde async_models.py (line 194) y main.py (line 702).
dispatch_light_validation_requests ahora hace healthcheck por validador una sola vez por proceso/orden de validación y cachea el resultado en memoria local de esa ejecución. Si falla, no envía validaciones a ese validador para ninguna aserción: main.py (line 482).
Añadí VALIDATOR_SERVICE_URL a los overlays local/prod de los workers para apuntar a validate-service-{n}.apis.svc.cluster.local:8070.
Verificación:

Pasó python3 -m py_compile api/news-handler/main.py api/validate-asertions/main.py api/common/models/async_models.py.
Nota: tras desplegar, los validators deben publicar/refrescar su config para que news-handler reciba service_url; el startup existente ya publica config cuando arranca el worker.
    



Develop v0.0.12.
- integración con X/Trhreads

Develop v0.0.13.   Reputation policy
    - consulta de reputaciones. Consulta de nombre, tipo y reputacion
    - gestión de reputaciones fuera de la cadena. 
        AI defecto reputacion media
        Especializaciones maxima
        individuales minima o custom
- estado validated --> dejar en pending y solo cerrar si todas validadas o al cabo de x tiempo




Develop v0.0.14.
- integracion con llm dedicado de categoria concreta

Develop v0.0.15.
- loki persistente
- Hardering frontend
- restylish frontend

