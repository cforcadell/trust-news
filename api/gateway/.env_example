


# =================================================
# Mistral AI
# =================================================
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-tiny
MISTRAL_API_URL=https://api.mistral.ai/v1/chat/completions

# =================================================
# GEMINI AI
# =================================================
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta

# =================================================
# OPENROUTER AI
# =================================================
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.5-flash
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions

# =================================================
# Prompt  
# =================================================
# =================================================
# Prompt  
# =================================================
# PROMPT='Extrae solo las aserciones verificables que contengan cifras objetivables y eliminen cualquier valoración subjetiva. 
# Separa las afirmaciones de conceptos diferentes en frases individuales pero que incluyan contexto (breve). Aquellas frases que hablen del mismo concepto unificalas en las misma aserción! 
# Formatea el resultado como una lista de aserciones en este formato  [
#     {
#         "idAssertion": "int",
#         "text": "string",
#         "categoryId": "int"
#     }
# ]
# idAssertion debe ser un identificador único NUMERICO (sin caracteres alfanumericos)para cada aserción (comienza en 1 y aumenta secuencialmente. Espero: 1,2,3,...).
# Los valores de las categorias son: 1,ECONOMÍA; 2,DEPORTES; 3,POLÍTICA; 4,TECNOLOGÍA; 5,SALUD; 6,ENTRETENIMIENTO; 7,CIENCIA; 8,CULTURA; 9,MEDIO AMBIENTE; 10,SOCIAL;
# Si no hay aserciones verificables, devuelve lista vacía. El idioma de la respuesta debe ser el mismo que el del texto analizado. '

PROMPT="Extrae únicamente aserciones verificables con datos cuantificables
(cifras, porcentajes, cantidades, fechas o magnitudes) o verificables de forma factual.
Excluye opiniones, valoraciones, predicciones o lenguaje subjetivo.

Cada aserción debe ser una frase completa, autocontenida
y con el contexto mínimo necesario para su verificación.

Separa hechos o conceptos distintos en aserciones independientes.
Unifica en una sola aserción las frases que describan el mismo hecho
si son complementarias y no contradictorias.
No dupliques aserciones equivalentes.

Clasifica cada aserción en una única categoría usando solo estos valores:
1 ECONOMÍA, 2 DEPORTES, 3 POLÍTICA, 4 TECNOLOGÍA, 5 SALUD,
6 ENTRETENIMIENTO, 7 CIENCIA, 8 CULTURA, 9 MEDIO AMBIENTE, 10 SOCIAL.

Devuelve exclusivamente un array JSON válido con esta estructura exacta:
[{ \"idAssertion\": 1, \"text\": \"Texto de la aserción verificable\", \"categoryId\": 3 }]

idAssertion debe ser numérico, único y secuencial empezando en 1.
No añadas campos adicionales ni texto fuera del JSON.

Si no hay aserciones verificables, devuelve [].
El idioma de la salida debe ser el mismo que el del texto analizado."

HTTP_TIMEOUT = 60
MAX_RETRIES = 1
RETRY_DELAY = 1

# =================================================
# Kafka
# =================================================
KAFKA_BOOTSTRAP=kafka:9092
KAFKA_REQUEST_TOPIC="fake_news_requests_generate"
KAFKA_RESPONSE_TOPIC="fake_news_responses"

# =================================================
# Logging
# =================================================
LOG_LEVEL=INFO

AI_PROVIDER=gemini # Options: mistral, gemini

